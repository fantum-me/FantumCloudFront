enum View {
    FOLDER = "folder",

    FILES = "files",
    FAVORITES = "favorites",
    TRASH = "trash",

    MEMBERS = "members",
    PERMISSIONS = "permissions",
    SETTINGS = "settings",
}

export default View
